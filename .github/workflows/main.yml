name: Markdown to PDF

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-18.04
    steps:
      - uses: docker://pandoc/core:2.9
        with:
          args: "-f html -t markdown https://github.com/Sae86/hello-github-actions.wiki.git -o test.pdf"
